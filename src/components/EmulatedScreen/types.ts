export type EmulatorContainer = {
    sizeX: number,
    sizeY: number,
    resolutionX: number,
    resolutionY: number,
}

export type EmulationSettings = {
    intergerScalling: boolean,
    overscan: boolean,
}